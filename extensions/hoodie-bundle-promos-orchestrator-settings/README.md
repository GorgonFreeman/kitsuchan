# Hoodie Bundle Promos Orchestrator — Admin settings

UI extension target: `admin.discount-details.function-settings.render`.

Writes `$app` / `function-configuration` JSON consumed by the discount function.
